apiVersion: v1
kind: Service
metadata:
  name: mlflow
  namespace: smartqueue-platform
  labels:
    app: mlflow
spec:
  type: NodePort
  selector:
    app: mlflow
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30500
